# claude-test リポジトリ

## ⚠️ このリポジトリは公開Webサイトを配信している

GitHub Pages により、以下が**公開・稼働中**です：

**https://hidetaka5555-max.github.io/claude-test/** — ゴールデンラビットビール四條畷醸造所トップページ

- `index.html` と `images/`（17枚）が**サイトの実体**。消すとサイトが落ちる
- リポジトリは**公開設定**。無料プランのため、非公開にしてもPagesが止まる
- ここに置くファイルは誰でも見られる。未発表の企画名・取引先名を含むものを置かない

## 成果物の置き場所

原則は「生成物は `~/Desktop/AIの作業場/` へ、リポジトリはコードと設定のみ」。
**ただし `index.html` と `images/` はサイトのソースなので例外**として、このリポジトリに置く。

2026-08-09にこの区別を取り違え、サイトのファイルをまとめてデスクトップへ移動して
リポジトリから削除するコミット（`498c23f`）を作ってしまった経緯がある。
そのコミットは push されず、2026-08-20に `origin/main` を土台として作り直した。

完成した成果物をデスクトップへ届けるときは、手で `cp` せず専用スクリプトを使う:

```bash
scripts/deliver-to-desktop.sh <コピー元> <作業場からの相対パス>
```

このスクリプトは `~/Desktop/AIの作業場/` の外へは絶対に書き込まない。

## 作業場

`~/Desktop/AIの作業場/` に案件ごとのフォルダがある。
**構成はこのファイルに書かない** — 公開リポジトリなので、未発表の企画名や
取引先名が外から見えてしまう。実際の構成は記憶システム側に記録してある。

## バックアップ

外付けSSD `SSD-PHPU3A` に自動保存（2時間ごと／日次）。
`~/scripts/backup_to_ssd.sh` ＋ LaunchAgent `com.oyatsumugi.claude-backup`。
このSSDはExFATのため、**フォルダのコピーではなく tar 書庫**で保存すること
（日本語名のUnicode正規化差で、コピーすると削除も修復もできなくなる）。
